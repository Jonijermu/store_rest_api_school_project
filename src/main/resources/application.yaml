spring:
  application:
    name: store_rest_api_school_project

  datasource:
    url: jdbc:mariadb://localhost:3306/store_test
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    show-sql:
      true
  sql:
    init:
      mode: always

